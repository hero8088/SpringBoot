<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="Members">
	<select id="findByEmail" parameterType="String" resultType="com.example.dto.Account"> 
	SELECT SYSID, ROLE, PASSWORD, NAME, EMAIL, ALIAS FROM MEMBERS
	WHERE EMAIL = #{email}
	</select> 
	
</mapper>

